import React from "react";

/**
 * Renders a not found page. Used when no intended route matched
 * @returns {Component} a React component
 */
const NotFound = () => (
  <div>
    <h2>Not Found!</h2>
  </div>
);

export default NotFound;
